const assert = require('assert');


describe('this is a dummy test', function() {
    it('invoke should not fail when running mocha tests 1', function() {
        assert.equal(100, 100)
    });
    it('invoke should not fail when running mocha tests 2', function() {
        assert.ok(true)
    });
});
